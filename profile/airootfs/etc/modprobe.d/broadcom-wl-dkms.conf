# broadcom-wl-dkms blacklists several in-kernel Broadcom drivers by default.
# The live ISO must support diverse hardware, so this empty same-named override
# keeps those drivers available instead of forcing wl globally.
#
# On hardware that specifically requires wl, remove this file, unload any
# conflicting Broadcom modules, and then load wl.
